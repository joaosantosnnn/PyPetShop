import React from'react';import{usePetGestor}from'../../context/AppContext';import{saveCancellationPolicy}from'../../services/refundRepository';export const PaidCancellationPolicy:React.FC=()=>{const{company,addToast}=usePetGestor();const save=async(e:React.FormEvent<HTMLFormElement>)=>{e.preventDefault();const f=new FormData(e.currentTarget);try{await saveCancellationPolicy(company.id,String(f.get('policy')));addToast('Política de pagamento atualizada.','success')}catch(x){addToast(x instanceof Error?x.message:'Erro.','error')}};return <form onSubmit={save} className="bg-white dark:bg-slate-900 border rounded-2xl p-4 mb-6"><h3 className="font-bold">Cancelamento de agendamento pago</h3><select name="policy" defaultValue={(company as any).paid_cancellation_policy||'credito'} className="w-full p-2 border rounded-xl my-3"><option value="credito">Gerar crédito para o cliente</option><option value="estorno">Abrir solicitação de estorno Pix</option></select><button className="px-4 py-2 bg-teal-600 text-white rounded-xl font-bold">Salvar política</button></form>}
