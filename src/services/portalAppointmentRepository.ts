import{supabase}from'../lib/supabase';const fail=(e:{message:string}|null)=>{if(e)throw new Error(e.message)};export async function cancelPortalAppointment(id:string,reason:string){const{error}=await supabase.rpc('cancel_portal_appointment',{p_appointment:id,p_reason:reason});fail(error)}export async function reschedulePortalAppointment(id:string,slot:string,reason:string){const{error}=await supabase.rpc('reschedule_portal_appointment',{p_appointment:id,p_slot:slot,p_reason:reason});fail(error)}
