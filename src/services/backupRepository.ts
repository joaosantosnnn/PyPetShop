import{ supabase }from'../lib/supabase';const fail=(e:{message:string}|null)=>{if(e)throw new Error(e.message)};export async function exportBackup(){const{data,error}=await supabase.rpc('export_company_backup');fail(error);return data}export async function importCatalog(value:unknown){const{data,error}=await supabase.rpc('import_company_catalog',{p_data:value});fail(error);return data as{customers:number;services:number;products:number}}
